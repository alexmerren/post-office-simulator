#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "service.h"

/* Function Prototypes */
extern int  getParamValue       (char*, char[]);
extern void createOutputFile    (char*);
extern void checkParams         (int);